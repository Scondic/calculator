import { cn } from '@bem-react/classname';import './index.scss';const cnSwitchButton = cn('SwitchButton');interface ISwitchButtonProps {  isActive: boolean;  iconLeft?: React.ReactElement;  iconRight?: React.ReactElement;  onClick?: React.MouseEventHandler<HTMLButtonElement>;}export const SwitchButton = ({  children,  isActive,  iconLeft,  iconRight,  onClick}: React.PropsWithChildren<ISwitchButtonProps>) => {  return (    <>      <button className={cnSwitchButton({ isActive })} onClick={onClick}>        {iconLeft && <span className={cnSwitchButton('Icon')}>{iconLeft}</span>}        {children}        {iconRight && <span className={cnSwitchButton('Icon')}>{iconRight}</span>}      </button>    </>  );};